spring:
  application:
    name: secure-api

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080

jwt:
  expiration: 3600000   # 1 hora (milisegundos)